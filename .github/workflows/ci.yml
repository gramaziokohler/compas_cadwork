name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ci:
    if: "!contains(github.event.pull_request.labels.*.name, 'docs-only')"
    runs-on: ubuntu-slim
    steps:
      - name: Setup uv
        uses: josemmo/compas-actions.build@feat/uv-support
        with:
          python: 3.12
          management_tool: uv
          invoke_lint: false
          check_import: false
          invoke_test: false

      - name: Run linter
        run: uv run inv precommit

      - name: Run tests
        run: uv run inv test

      - name: Build project
        run: uv build
